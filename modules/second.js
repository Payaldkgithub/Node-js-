const {add,sub}=require("./first");
let x=500,y=100;
let sum=add(x,y);
console.log(`Addition of ${x} and ${y} is ${sum}`);
let minus=sub(x,y);
console.log(`subtraction  of ${x} and ${y} is ${minus}`);